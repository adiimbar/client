// NOT IN USE


// import { AbstractControl } from "@angular/forms";

// export function getErrorEmail(control: AbstractControl): {[key: string]: any } | null {
//     const email = control.get('email');


// //   getErrorEmail() {
//     return email.hasError('pattern') ? 'Not a valid email address' : '';
//         // this.registrationForm.get('email').hasError('alreadyInUse') ? 'This email address is already in use' : '';
//   }
